import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.pocketfox.companion',
  appName: 'PocketFox Companion',
  webDir: 'dist',
}

export default config
